import 'package:flutter/material.dart';

class ProfessorItem extends StatelessWidget {
  const ProfessorItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}